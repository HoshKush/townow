<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="category">
	<insert id="create" parameterType="CategoryDTO">
		insert into category(ca1_name, ca2_name)
		values(#{ca1_name, ca2_name)
	</insert>
	
	<delete id="delete" parameterType="int">
		delete from category
		where ca_id = #{pk}
	</delete>
	
	<select id="list" parameterType="Map" resultType="CategoryDAO">
		select ca_id, ca1_name, ca2_name, @ROWNUM:=@ROWNUM + 1
		from(
			select ca_id, ca1_name, ca2_name, @ROWNUM:=0 
			from(
				select ca_id, ca1_name, ca2_name from category
				<where>
					<if test="ca1_name!=null">
						ca1_name = #{ca1_name}
					</if>
				</where>
				))
	</select>
	
	<select id="total" parameterType="Map" resultType="int">
		select count(*) from category
		<where>
			<if test="ca1_name!=null">
				ca1_name = #{ca1_name}
			</if>
		</where>
	</select>
</mapper>